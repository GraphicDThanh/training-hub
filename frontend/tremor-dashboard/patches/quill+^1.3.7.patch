# generated by patch-package 6.4.14
#
# declared package:
#   quill: ^1.3.7
#
diff --git a/node_modules/quill/dist/quill.js b/node_modules/quill/dist/quill.js
index 811b3d0..d8a64c4 100644
--- a/node_modules/quill/dist/quill.js
+++ b/node_modules/quill/dist/quill.js
@@ -4234,7 +4234,11 @@ var Scroll = function (_Parchment$Scroll) {
       }, {});
     }
     // Some reason fixes composition issues with character languages in Windows/Chrome, Safari
-    _this.domNode.addEventListener('DOMNodeInserted', function () {});
+    // _this.domNode.addEventListener('DOMNodeInserted', function () {});
+    const observer = new MutationObserver(mutationList =>  
+      mutationList.filter(m => m.type === 'childList').forEach(m => {  
+        m.addedNodes.forEach(doSomething);  
+      }));  
     _this.optimize();
     _this.enable();
     return _this;
@@ -5513,6 +5517,7 @@ var Picker = function () {
     key: 'buildLabel',
     value: function buildLabel() {
       var label = document.createElement('span');
+      label.setAttribute('aria-label', 'picker-label');
       label.classList.add('ql-picker-label');
       label.innerHTML = _dropdown2.default;
       label.tabIndex = '0';
@@ -9526,6 +9531,8 @@ Toolbar.DEFAULTS = {};
 
 function addButton(container, format, value) {
   var input = document.createElement('button');
+  input.setAttribute('aria-label', ('ql-' + format));
+  input.style.margin = "5px";
   input.setAttribute('type', 'button');
   input.classList.add('ql-' + format);
   if (value != null) {
@@ -9983,6 +9990,7 @@ var SnowTooltip = function (_BaseTooltip) {
         }
         event.preventDefault();
       });
+      this.root.querySelector('a.ql-action').setAttribute('href', '#');
       this.root.querySelector('a.ql-remove').addEventListener('click', function (event) {
         if (_this3.linkRange != null) {
           var range = _this3.linkRange;
@@ -9993,6 +10001,7 @@ var SnowTooltip = function (_BaseTooltip) {
         event.preventDefault();
         _this3.hide();
       });
+      this.root.querySelector('a.ql-remove').setAttribute('href', '#');
       this.quill.on(_emitter2.default.events.SELECTION_CHANGE, function (range, oldRange, source) {
         if (range == null) return;
         if (range.length === 0 && source === _emitter2.default.sources.USER) {
